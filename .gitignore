# Ignore files with .backup extension
*.backup

# Ignore credentials.auto.tfvars file (contains sensitive credentials)
credentials.auto.tfvars

# Ignore all files with .tfstate extension (Terraform state files)
*.tfstate
*.tfstate.*

# Ignore the .terraform directory (contains Terraform runtime files)
.terraform
